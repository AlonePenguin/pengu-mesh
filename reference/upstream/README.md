# Upstream References

This directory keeps compact metadata for external projects that pengu mesh
uses as comparison baselines.

The public repository intentionally does not vendor full upstream source trees.
That keeps this project smaller, easier to audit, and free of unrelated test
fixtures, placeholder secrets, and release workflows.

Current baseline:

- `pinchtab.METADATA.json`: PinchTab repository, commit, and public tree URL
